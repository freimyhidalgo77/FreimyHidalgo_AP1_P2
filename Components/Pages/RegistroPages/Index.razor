@page "/Combo/Index"
@inject ComboDetalleService comboDetalleService
@inject NavigationManager navigation

<PageTitle>Registro</PageTitle>

<div class="container card shadow-lg">
    <div class="card-header text-center">
        <h3><strong>Registro</strong></h3>
    </div>

    <!-- Header -->
    <div class="row mb-3 align-items-end">
        <div class="col-md-3">
            <label for="Filtro" class="form-label"><strong>Filtrar por:</strong></label>
            <InputSelect id="filtro" class="form-select" @bind-Value="filtro">
                <option value="" disabled>Seleccionar</option>
                <option value="ComboId">Registro ID</option>
                <option value="descrpcion">Descripcion</option>
                <option value="precio">Precio</option>

            </InputSelect>
        </div>

        <div class="col-md-6">
            <label for="ValorFiltro" class="form-label"><strong>Búsqueda:</strong></label>
            <div class="input-group">
                <input @bind="ValorFiltro" class="form-control" placeholder="Buscar" />
                <button @onclick="Buscar" class="btn btn-success" type="button">
                    <span class="bi bi-search"></span> Buscar
                </button>
                <a href="/Combo/Create" class="btn btn-success bi bi-plus-square-fill ms-2"> Crear</a>
            </div>
        </div>
    </div> 
</div>

<!-- Body -->
<div class="card-body"> 
    <table class="table table-light table-bordered">
        <thead class="table table-striped text-center table-bordered mt-3">
            <tr>
                <th>Combo ID</th>
                <th>Fecha</th>
                <th>Descripcion</th>
                <th>Precio</th>
                <th>Ver</th>
                <th>Editar</th>
                <th>Eliminar</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var combo in combos)
            {
                <tr>
                    <td class="text-center">@combo.ComboId</td>
                    <td class="text-center">@combo.Fecha</td>
                    <td class="text-center">@combo.descripcion</td>
                    <td class="text-center">@combo.precio</td>
             
                    <td class="text-center">
                    <a href="/Combo/Details/@combo.ComboId" class="btn btn-outline-warning bi bi-eye-fill"></a>
                    </td>
                    <td class="text-center">
                        <a href="/Combo/Edit/@combo.ComboId" class="btn btn-outline-warning bi bi-pencil-square"></a>
                    </td>
                    <td class="text-center">
                        <a href="/Combo/Delete/@combo.ComboId" class="btn btn-outline-danger bi bi-trash"></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <div class="card-footer">
        <p><strong>Total de Registros de combos: </strong>@combos?.Count</p>
    </div>
</div>



@code {
    public List<Combo> combos { get; set; } = new List<Combo>();
    public string filtro { get; set; } = string.Empty;
    public string ValorFiltro { get; set; } = string.Empty;


    private async Task Buscar()
    {
        if (ValorFiltro.Trim() != "")
        {
            if (filtro == "RegistroId")
            {
                //   Registro = await registroService.Listar(e => e.Registro.NombreRegistro.ToLower().Contains(ValorFiltro.ToLower()));
            }

        }

    }
}
